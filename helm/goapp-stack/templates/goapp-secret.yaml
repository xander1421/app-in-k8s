apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-goapp-secret
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "goapp-stack.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Note: PostgreSQL credentials are managed by CloudNativePG operator
  # The deployment references the operator-created secret directly
  # Redis password (if configured)
  REDIS_PASSWORD: ""
